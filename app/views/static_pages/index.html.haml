= content_for :title do
  Europeana · Pykih
.container.index
  .row
    .col-sm-3
      .well.login-well
        %h5.thin Sign In
        %br
        = simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "form"}) do |f|
          = f.input :username, autofocus: true, required: true, input_html: {style: "width: 100%;"}
          = f.input :password, as: :password, autocomplete: "off", required: true, input_html: {style: "width: 100%;"}
          = f.button :submit, "Sign in"
        %br
        = link_to "Can't access your account?", new_password_path(resource_name), style: "font-size: 11px; color: blue;"
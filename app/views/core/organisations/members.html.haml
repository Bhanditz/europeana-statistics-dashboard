- content_for :title do
  People · #{@account.username}

.row
  .col-sm-2= render partial: "accounts/sidebar"
  .col-sm-10
    .panel.panel-default
      .panel-heading= render partial: "core/organisations/topbar"
      .panel-body
        %br
        %table.table.table-condensed.table-striped
          %thead
            %tr
              %th Member
              %th Sign In Count
              %th Last Signed In
              %th Currently Logged In?
          %tbody
            - if @accounts.first.present?
              - @accounts.each do |account|
                %tr
                  %td= link_to account.username, _account_path(account)
                  %td= account.sign_in_count
                  %td= time_ago_in_words(account.current_sign_in_at)
                  %td= account.is_online ? online : offline
            - if @invites.first.present?
              - @invites.each do |invite|
                %tr
                  %td= invite
                  %td
                  %td
